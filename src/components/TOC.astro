---
interface Heading {
  depth: number;
  slug: string;
  text: string;
}

interface Props {
  headings: Heading[];
}

const { headings } = Astro.props;

// Filter to only show h2 and h3
const tocHeadings = headings.filter((h) => h.depth >= 2 && h.depth <= 3);
---

{tocHeadings.length > 0 && (
  <nav class="aside-panel" aria-label="Table of contents">
    <h2 class="aside-panel-title">Contents</h2>
    <ol class="aside-panel-list">
      {tocHeadings.map((heading) => (
        <li class="toc-item" data-depth={heading.depth}>
          <a href={`#${heading.slug}`} class="toc-link">
            {heading.text}
          </a>
        </li>
      ))}
    </ol>
  </nav>
)}

<style>
  .aside-panel {
    overflow-y: auto;
  }

  .aside-panel-list {
    border-left: 1px solid var(--color-border);
  }

  .toc-item {
    margin-bottom: 0;
  }

  .toc-item[data-depth="3"] {
    padding-left: theme(spacing.3);
  }

  .toc-link {
    display: block;
    padding: theme(spacing.1) theme(spacing.2);
    color: var(--color-text-muted);
    text-decoration: none;
    line-height: var(--leading-snug);
  }

  .toc-link:hover {
    color: var(--color-link);
  }

  .toc-link.active {
    color: var(--color-text);
    font-weight: 700;
    background-color: var(--color-bg-subtle);
  }
</style>

<script>
  import '@/scripts/toc-scrollspy';
</script>
